name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    env:
      PYTHONPATH: ${{ github.workspace }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install DVC
        run: pip install dvc

      - name: Pull data (required for tests)
        run: |
          dvc pull -r origin --force
          dvc checkout

      - name: Verify dataset exists
        run: ls data/


      - name: Run tests
        run: pytest tests/ -v


  train:
    needs: test
    runs-on: ubuntu-latest

    env:
      PYTHONPATH: ${{ github.workspace }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install DVC
        run: pip install dvc

      - name: Pull data (required for tests)
        env:
          DVC_HTTP_BASIC_LOGIN: ${{ secrets.DVC_USER }}
          DVC_HTTP_BASIC_PASSWORD: ${{ secrets.DVC_PASSWORD }}
        run: |
          dvc pull --force
          dvc checkout

      - name: Verify dataset exists
        run: ls data/

      - name: Train model
        run: python src/train.py